id: code-review-en
description: >-
  P11-T1 smoke fixture — runs the "code-review" recipe end to end
  through the real planWithRecipe -> validatePlan -> runScheduler chain
  against MockLLMProvider. Proves the harness works for an English-language
  request; not one of P11-T2's required 12 golden tasks (those are not
  written yet).
tags: [small, code, analysis, en]
recipeName: code-review
userRequest: "Review the changed files in this branch against our review checklist and summarize findings"
budgetTotal: 1000000
budgetLevel: standard
understanding:
  intent: analyze
  deliverableShape:
    kind: markdown
    estimatedSize: medium
    structure: sectioned
  evidenceNeeds: []
  acceptanceCriteria:
    - "review summary covers every changed file"
  ambiguities: []
  riskFlags: []
assertions:
  maxTokensSpent: 50000
  maxDurationMs: 5000
